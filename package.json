{
  "name": "academix-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd chatbot-frontend && npm install && npm run build && cd .. && cp -r chatbot-frontend/build ./build"
  }
}
